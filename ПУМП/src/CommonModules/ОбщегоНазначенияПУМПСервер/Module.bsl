Функция ПолучитьПапкуДанных() Экспорт
	Перем пПапка;
	пПапка="";
	Если ОбщегоНазначения.ЭтоWindowsСервер() Тогда
		пПапка=Константы.ПапкаСервераWindows.Получить();
	ИначеЕсли ОбщегоНазначения.ЭтоLinuxСервер() Тогда
		пПапка=Константы.ПапкаСервераLinux.Получить();
	КонецЕсли;
	Возврат пПапка;
КонецФункции

